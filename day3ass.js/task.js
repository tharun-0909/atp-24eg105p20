import { validatetasktitle,validatepriority,validateduedate} from "./validator.js";
// 1. Add new task
                    function addTask(title, priority, dueDate) {
                      if(!titel && !priority && !duedate){
                        
                      }
                    }
